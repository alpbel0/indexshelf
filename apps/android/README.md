# Android

Kotlin/Compose Android client using a single `:app` module. The project targets
Android API 35, supports API 26+, and uses Hilt for application dependency
injection. Cleartext traffic and application backup are disabled by default.

```text
gradlew.bat assembleDebug
gradlew.bat lint
gradlew.bat test
```
